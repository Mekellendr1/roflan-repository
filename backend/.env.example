# ============================================================
# WorkTime Sync — переменные окружения
# Скопируй файл в .env и подставь свои значения.
# ============================================================

# --- База данных ---
# Основной режим: Postgres
DATABASE_URL=postgresql+psycopg2://worktime:worktime@localhost:5432/worktime

# Альтернатива: SQLite-файл (для быстрого локального запуска без Postgres)
# DATABASE_URL=sqlite:///./worktime.db

# --- AI-ассистент ---
# Ключ OpenAI (https://platform.openai.com/api-keys).
# Без него /ai/* эндпоинты вернут 502.
OPENAI_API_KEY=

# Модель по умолчанию — gpt-4o. Можно указать gpt-4o-mini / gpt-4-turbo и т.д.
OPENAI_MODEL=gpt-4o
